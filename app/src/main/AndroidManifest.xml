<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doitapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".menu.AddProductActivity"></activity>
        <activity android:name=".menu.MenuBigbarActivity" />
        <activity android:name=".menu.chococrunchactivity" />
        <activity android:name=".menu.kurmondactivity" />
        <activity android:name=".menu.bigbaractivity" />
        <activity android:name=".log.ResetPasswordActivity" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".log.RegisterActivity" />
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity android:name=".log.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>